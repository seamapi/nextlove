{
  "name": "nextlove-turborepo",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "start:example": "cd apps/example-todo-app && yarn start",
    "test": "cd packages/nextlove && yarn test",
    "generate-openapi:example": "cd apps/example-todo-app && yarn build:openapi",
    "publish-packages": "cd packages/nextlove && yarn build && npm publish"
  },
  "devDependencies": {
    "eslint-config-custom": "*",
    "prettier": "^2.7.1",
    "turbo": "latest"
  },
  "engines": {
    "npm": ">=7.0.0",
    "node": ">=14.0.0"
  },
  "dependencies": {},
  "packageManager": "yarn@1.22.17",
  "repository": "https://github.com/seamapi/nextlove"
}
